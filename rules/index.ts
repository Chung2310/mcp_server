import { ANALYSIS_RULE } from "./analysis.rule";
import { DEV_RULE } from "./dev.rule";
import { TEST_RULE } from "./test.rule";

export const RULES = {
  analysis: ANALYSIS_RULE,
  dev: DEV_RULE,
  test: TEST_RULE,
};